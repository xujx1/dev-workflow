# 04-archive — 需求跟踪报告结构

> 由 `SKILL.md` 按需 Read。

---

## 6 节结构

```
核心指标汇总表（顶部）
节1：MRD 标准度（6 维度打分）
节2：AI PRD 功能覆盖度
节3：AI 技术方案覆盖度
节4：AI 代码采纳率（git diff 计算）
节5：测试报告摘要（JaCoCo 覆盖率）
节6：命中追踪（基于主要读取路径的自然语言总结）
```

---

## tech-only 模式适配

当 `execution-state.md` 中 `mode=tech-only` 时：

- 节1（MRD 标准度）：写为 `N/A（tech-only 模式，无 MRD 流程）`
- 节2（AI PRD 功能覆盖度）：写为 `N/A（tech-only 模式，无 PRD 流程）`
- 节3～6：正常填写，不受影响

---

## 节6 附加要求

除自然语言总结外，核心指标表必须补齐 3 条阶段知识库命中率：

- `PRD 生成知识库命中率`：**tech-only 模式下写为 `N/A`**
- `技术方案知识库命中率`
- `测试用例知识库命中率`

统一按"知识资产路径数 / 主要读取路径总数"计算。

---

## 报告头部必填项

- `AI 生成 commit`：`{ai_commit_hash}`
- `归档代码快照`：`{archive_code_ref}`
- 若两者不同，补一行：`归档前存在追加提交，本报告与知识库均以当前分支最新代码快照为准`
