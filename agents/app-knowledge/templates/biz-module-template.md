# {模块名} 业务知识

> 所属服务：{服务名}
> 更新时间：{YYYY-MM}

---

## 模块定位

{模块核心职责，≤40字。示例：承运商匹配与自定义下单，负责选择合适承运商并创建运单。}

**业务流程节点**：{前驱模块} → **{本模块}** → {后继模块}

---

## 关键接口（本模块涉及）

| 接口 | QPS | P99 | 用途 |
|------|-----|-----|------|
| `{接口类名}/{方法名}` | {N} | {X}ms | {一句话用途} |

---

## 业务流程

```
{用 ASCII 图表达主流程，建议 10-20 行}

示例：
用户发起下单
    │
    ├─ 参数校验
    │   ├─ 校验失败 → 返回 4xx
    │   └─ 校验通过 ↓
    ├─ 承运商匹配（matchCarrier）
    │   ├─ 无可用承运商 → 抛 NoCarrierException
    │   └─ 匹配成功 ↓
    ├─ 调用承运商下单 API
    │   ├─ 超时/失败 → 重试3次 → 写失败队列
    │   └─ 成功 → 返回运单号
    └─ 写 DB + 发 MQ
```

---

## 核心业务规则

> 只写影响代码行为的规则，不是功能描述。

1. {规则-1}：{说明}
2. {规则-2}：{说明}
3. {规则-3}：{说明}

---

## 状态机（如有）

| 状态 | 含义 | 触发转换的事件 |
|------|------|-------------|
| {STATUS_A} | {含义} | {事件} → {STATUS_B} |
| {STATUS_B} | {含义} | {事件} → {STATUS_C} |

---

## 关键代码路径

| 位置 | 说明 |
|------|------|
| `{包路径}/{类名}.java:{行号}` | {入口/核心方法} |
| `{包路径}/{类名}.java:{行号}` | {关键业务逻辑} |

---

## 依赖数据

| 数据源 | 表/Key | 读/写 | 说明 |
|--------|--------|-------|------|
| MySQL | `{表名}` | R/W | {核心字段：id, status, ...} |
| Redis | `{key前缀}:{id}` | R | {用途，TTL: Xs} |

---

## 注意事项

- {特殊逻辑说明，如"匹配逻辑有降级策略，当主承运商不可用时自动切备选"}
- {已知风险，如"该接口 P99=5s，高并发时有超时风险，接入方需设合理超时"}
