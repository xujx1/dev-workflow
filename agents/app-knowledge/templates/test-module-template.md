# {接口类名}/{方法名} 测试用例

> 接口文档：[{编号}_{接口标识}](../api-docs/{编号}_{接口标识}.md)
> 更新时间：{YYYY-MM}

---

## 接口概况（测试视角）

| 项 | 内容 |
|----|------|
| 接口类型 | Dubbo / HTTP |
| 峰值 QPS | {N} |
| P99 | {X}ms |
| 幂等性 | 是 / 否（幂等键：{字段名}） |
| 事务范围 | 无 / 单表 / 跨表（{说明}） |

---

## 测试边界分析

> 快速定位：哪些边界最容易出 Bug？

| 边界 | 输入特征 | 预期行为 |
|------|---------|---------|
| 正常路径 | 合法参数 | 返回正确结果 |
| 空值/null | {参数}=null | 返回 {错误码} |
| 超边界值 | {参数}>{最大值} | 抛 {XxxException} |
| 并发场景 | 同一{幂等键}并发请求 | 仅处理一次，其余幂等返回 |
| 外部依赖失败 | {外部服务}超时 | {降级/重试/失败} |

---

## 测试用例

### TC-01 正常路径

**前置条件**：{DB状态/数据准备}

**输入**：
```json
{
  "{param1}": "{value1}",
  "{param2}": "{value2}"
}
```

**预期输出**：
```json
{
  "success": true,
  "data": { "{key}": "{expected_value}" }
}
```

**验证点**：
- [ ] 返回 success=true
- [ ] {key} 字段值正确
- [ ] DB 中 {表名}.{字段} 已更新

---

### TC-02 参数校验失败

**输入**：`{param1}` 为 null

**预期**：抛出 `{XxxBizException}`，错误码 `{ERR_CODE}`

---

### TC-03 幂等验证

**操作**：以相同 `{幂等键}={value}` 调用两次

**预期**：两次均返回成功，数据库仅写入一条记录

---

### TC-04 外部依赖降级

**模拟**：Mock `{外部服务类}` 抛出超时异常

**预期**：{降级行为描述，如"返回默认值/走本地缓存/返回失败错误码"}

---

## Mock 策略

| 依赖 | Mock 方式 | 说明 |
|------|----------|------|
| `{外部DubboService}` | `@MockBean` | {何时Mock，Mock返回值} |
| `{Mapper类}` | H2内存库 / `@MockBean` | {使用真实DB或Mock} |
| `{Redis}` | `embedded-redis` | {测试环境Redis配置} |

---

## 已知测试盲区

- {不易测试的场景，如"MQ消费延迟触发的状态变更，需要集成测试覆盖"}
- {平台限制，如"APM埋点在单测中不生效，需真实环境验证"}
